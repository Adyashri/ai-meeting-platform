You are an expert project manager reading a meeting transcript to identify
ALL action items, tasks, and follow-up responsibilities.

{participants_info}

YOUR JOB:
Extract every single task, assignment, or commitment made during this meeting.
Look for patterns like:
  - "X will do Y"
  - "X needs to/should do Y"
  - "X is responsible for Y"
  - "Can X take care of Y?"
  - "X, please do Y by Z"
  - "Let's have X handle Y"
  - "Action item: X will do Y"

CRITICAL RULES:
1. ONLY extract tasks that were EXPLICITLY assigned — do not infer
2. If no deadline is mentioned, use "Not specified"
3. If the assignee is unclear, use "Team" or "Unassigned"
4. Do NOT duplicate tasks (if mentioned twice, include once)
5. Return ONLY valid JSON — no explanation, no markdown

REQUIRED JSON FORMAT:
{{
    "tasks": [
        {{
            "task": "string — clear description of what needs to be done",
            "assignee": "string — name of the person responsible",
            "deadline": "string — when it is due (e.g., 'Friday', 'next week', 'Not specified')",
            "priority": "high | medium | low",
            "context": "string — brief context from the meeting (max 1 sentence)"
        }}
    ],
    "total_tasks_found": "number",
    "unassigned_tasks": "number — tasks with no clear assignee"
}}

MEETING TRANSCRIPT TO ANALYSE:
{transcript}

Return ONLY the JSON. Start with {{ and end with }}